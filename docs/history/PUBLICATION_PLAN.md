# LaTeX 文稿与仓库整理计划

依据：用户已授权整理全部核心推导、编译 PDF、整理研究仓库，并 commit / push 至 StephenQSstarThomas/lzxphys。凭据仅交给 Git credential helper，不读取到输出，不纳入版本控制。远端为空，本地尚未初始化 Git。

- [x] 保存现有成果，创建分层目录：`src/` 主算法、`scripts/` 独立验证与构建、`tests/` 验收、`results/` 可复现报告、`paper/` LaTeX/PDF、`docs/notes/` 推导、`docs/history/` 历史交接审计、`references/` 原论文。
- [x] LaTeX 写成一份自包含中文研究笔记：归一化、有限规范变换、F-move 取向、几何链、一般闭式、全局有限分块证明、CS 类识别与换面自由度、独立棱长公式、解析校准。计算部分引用仓库代码、包与实际验证，不粘贴完整程序。
- [x] 调整 Python 包布局与固定输出路径，补 README、打包元数据、Makefile、忽略规则、参考材料说明。保留已有数学算法与历史记录的语义，修复迁移后的链接和运行命令。
- [x] 重跑单元测试、原基线、24 样本验证、独立棱长复算；编译 LaTeX，检查警告、交叉引用、公式溢出及实际 PDF 页面。
- [x] 审查提交文件清单与文稿一致性，确认忽略凭据、缓存及构建中间件。发布到用户指定的 `main`，提交与远端状态以 Git 记录和交付回复为准。

文稿文件：`paper/su2_cocycle.tex` 与 `paper/su2_cocycle.pdf`。引用所依托的原始论文，区分已证明的数学结果与数值精度限制。原始交接和审查日志作为历史归档，不把代理审查称作外部同行评审。

整理后的 `make verify` 实际退出 0；15 项测试全部通过，24 样本最大体积差约 `1.85e-13`，独立棱长复算最大差约 `3.85e-84`。Python wheel 构建成功；安装元数据与声明的 Python >=3.11 一致。文稿含 11 节正文、收敛级数附录、5 项参考文献，107 个标签及引用相互匹配；PDF 已完成文本与页面检查。
