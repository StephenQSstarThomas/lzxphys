# SU(2) 显式余循环执行计划

> 历史归档：以下保留当时的进度、测试数量与旧目录命令。当前运行方式见仓库根目录 README；文件链接已迁移。


依据：`SU2_omega_six_agent_handoff.md`、`SU2_handoff_audit.md` 和用户确认的物理推导主线。当前目录不是 Git 仓库；直接保存研究文件，不创建或提交无关仓库。使用 writing-plans 的依赖与核验原则；用户已授权开始执行，无需重复审批。

目标：优先完成 A 级一般位置有限 Li₂ 闭式；在相容几何链基础上，尝试完成 B 级的有限全局分块算法。

- [x] 1. 保存固定约定与链级物理推导到 `SU2_derivation.md`：说明 CS 平坦限制、边/面边界、五边形整数周期及二级类识别。
- [x] 2. 写 `test_su2_omega.py` 的独立已知体积与取向校验，先确认缺少实现时失败，再实现 `su2_omega.py` 的一般位置公式。使用 Murakami 定理 1.1，完整写出所有变量、边编号及分支。
- [x] 3. 以固定种子的 24 个一般样本对照原积分；用正负取向、校准族、小角、共轭及五边形核验，记录真实误差到 `SU2_validation.json`。困难积分单独提升阶数/精度，不静默剔除。
- [x] 4. 证明并实现全局有界锥填充：反足边用固定中点分段；异常三角面将边界锥到确定顶点；异常三维链将面边界锥到确定顶点。候选顶点取左平移的 `(1,t,t²,t³)`，用有限线性代数避免已有面的线性张成。先核查等变性、退化归一化、终止性和与一般位置的兼容性。
- [x] 5. 用精确有理齐次四元数判定退化分支，避免浮点阈值改动余循环代表。特殊函数用 mpmath，并提升近退化输入的工作精度。矩阵入口说明数值输入所代表的精确归一化四元数。
- [x] 6. 核验中心元、含重复累计顶点的五边形、普通降维、原点落入凸包及靠近分支集合。独立审查后收敛为最终公式、运行方式和“已证明/数值支持/限制”说明。

最终文件：`SU2_derivation.md`（公式与证明）、`su2_omega.py`（实现）、`test_su2_omega.py`（独立验收）、`validate_su2.py` 和 `SU2_validation.json`（可复现数值报告）。现有审计文件保留其历史范围。

验收：A 级及 B 级有限分块形式均已完成。14 项单元测试通过；24 组原积分交叉验证最大体积差 `1.8474e−13`；全部报告门控通过。限制为未提供区间算术误差证书，并须区分精确群元和浮点矩阵近似。
